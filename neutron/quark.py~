import numpy as np
import multiprocessing
import mido

import logging

from . import config

class MidiServer(object):

    def __init__(self, ):
        logging.info('>> QUARK MIDI OUTPUT: {}'.format(config.QUARK_MIDIPORT))
        outport = mido.open_output(config.QUARK_MIDIPORT)
